# Emergency Nearby - Интеграция с FastAPI Backend

## 🚀 Быстрый старт

Ваш фронтэнд **готов к подключению** к FastAPI бэкэнду с PostgreSQL!

### 1️⃣ Настройте URL бэкэнда

Откройте `/lib/api.config.ts` и измените:

```typescript
export const API_BASE_URL = 'http://localhost:8000/api';
```

### 2️⃣ Запустите фронтэнд

```bash
npm install
npm run dev
```

### 3️⃣ Подключите бэкэнд

Подробная инструкция в файле **`/BACKEND_SETUP.md`**

---

## 📁 Структура API файлов

```
/lib/
├── api.config.ts    # 🔧 Конфигурация URL (измените здесь)
├── types.ts         # 📦 TypeScript типы данных
├── api.ts           # 🌐 API клиент со всеми методами
└── mockData.ts      # 🧪 Тестовые данные (fallback)
```

---

## 🔑 API эндпоинты

### Аутентификация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход  
- `GET /api/auth/verify` - Проверка токена

### Инциденты
- `GET /api/incidents` - Все инциденты (✅ auth)
- `GET /api/incidents/public` - Публичная информация (❌ auth)
- `POST /api/incidents` - Создать
- `PUT /api/incidents/{id}` - Обновить
- `GET /api/incidents/by-person/{person_id}` - По персоне

### Персоны
- `GET /api/persons` - Все персоны
- `POST /api/persons` - Создать
- `PUT /api/persons/{id}` - Обновить

### Статистика
- `GET /api/statistics?start_date=...&end_date=...` - За период

---

## 💡 Как это работает

### ✅ С бэкэндом
1. Приложение отправляет запросы на ваш FastAPI сервер
2. Данные сохраняются в PostgreSQL
3. Показываются уведомления об успехе/ошибке

### ⚠️ Без бэкэнда (Offline mode)
1. Если бэкэнд недоступен - автоматический fallback
2. Используются локальные mock данные
3. Всё работает для разработки и демонстрации

---

## 📊 Модели данных

### Person (Персона)
```typescript
{
  id: string;
  registrationNumber: string;
  name: string;
  address: string;
  role: 'suspect' | 'witness' | 'victim';
  phone: string;
  email: string;
}
```

### Incident (Инцидент)
```typescript
{
  id: string;
  registrationNumber: string;
  type: string;
  description: string;
  location: string;
  date: string;
  severity: 'low' | 'medium' | 'high';
  involvedPersons: string[]; // массив ID персон
}
```

---

## 🛠️ Пример использования API

### В компонентах React:

```typescript
import { api } from '../lib/api';

// Получить все инциденты
const incidents = await api.incidents.getAll();

// Создать инцидент
const newIncident = await api.incidents.create({
  type: 'Theft',
  description: 'Stolen vehicle',
  location: '123 Main St',
  severity: 'high',
  involvedPersons: ['person-1', 'person-2']
});

// Обновить инцидент
const updated = await api.incidents.update('inc-123', {
  description: 'Updated description'
});
```

---

## 🔐 Аутентификация

### JWT токен сохраняется в localStorage

```typescript
// Вход
const response = await api.auth.login({
  username: 'officer',
  password: 'password123'
});

// Токен автоматически добавляется к каждому запросу
// в заголовке: Authorization: Bearer <token>
```

---

## 📝 Что нужно сделать на бэкэнде

1. **Создать PostgreSQL базу данных** с таблицами:
   - `persons`
   - `incidents`  
   - `incident_persons` (связь many-to-many)
   - `users` (для аутентификации)

2. **Реализовать FastAPI эндпоинты** (см. `/BACKEND_SETUP.md`)

3. **Настроить CORS** для вашего фронтэнд URL

4. **Настроить JWT аутентификацию**

---

## ✨ Особенности интеграции

### ✅ Уже реализовано на фронтэнде:
- Все API запросы с обработкой ошибок
- Автоматическая отправка JWT токена
- Fallback на локальные данные при недоступности бэкэнда
- Toast уведомления для пользователя
- TypeScript типы для всех данных

### 🎯 Что делает fallback:
- Позволяет разрабатывать без запущенного бэкэнда
- Показывает mock данные для демонстрации
- Сохраняет изменения локально в React state
- Выводит понятное сообщение пользователю

---

## 🐛 Отладка

### Логи в консоли
Все API запросы логируются в консоль браузера:
```
Failed to fetch data: Error: ...
```

### Проверка токена
```javascript
// В консоли браузера
localStorage.getItem('auth_token')
```

### Проверка запросов
Откройте DevTools → Network → фильтр "Fetch/XHR"

---

## 📞 Поддержка

**Полная документация:** `/BACKEND_SETUP.md`

Там вы найдёте:
- Пример FastAPI кода
- SQL схемы для PostgreSQL
- Настройка CORS
- Примеры всех эндпоинтов
- Troubleshooting

---

## 🎉 Готово!

Теперь фронтэнд и бэкэнд могут легко общаться друг с другом!

**Измените только 1 строку** в `/lib/api.config.ts` и всё заработает! 🚀
